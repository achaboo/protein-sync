# Protein Sync — 作業メモ

体重と運動量から1日のたんぱく質目標を決め、その日の献立・時間帯・食費・プロテインの杯数まで
設計する単一ファイルのツール。公開先は https://achaboo.github.io/protein-sync/
**プロテインの製品は不定期に変わる。いまは Bpro。**

## ファイル

| ファイル | 中身 |
|---|---|
| `index.html` | ツール本体。HTML・CSS・JSをすべて内包（約4,300行）。**プロテインの製品は先頭の `PROTEIN` ブロックだけで切り替わる** |
| `README.md` | 計算ロジック・定数・判定基準・1週間の型の結果。**変更のたびに必ず同期する** |
| `apple-touch-icon.png` / `favicon-192.png` | アイコン |
| `tools/crosscheck.js` | README↔コードの機械照合。`node tools/crosscheck.js`（`-v` で照合した表の一覧）。食い違いがあれば終了コード1 |
| `tools/browser-check.js` | ランダム検証のハーネス。ブラウザのコンソールに貼って `PSCheck.run(1000)`。終わったら `PSCheck.restore()` |
| `tools/review-prompt.md` | 他のAIに科学的妥当性を見てもらうためのプロンプト。`---` から下をコピーして貼る。**設計・実測値・既知の弱点を書いてあるので、ツールを変えたらここも直す** |

## 崩してはいけない前提

- **HTML1枚で完結**。外部ライブラリ・CDN・ビルド手順なし。`file://` でも GitHub Pages でも動く
- **オフラインで動く**。通信を前提にした機能を足さない
- **サーバーを持たない**。保存は `localStorage`（キー `savas-calc-v3`）。端末間は「設定リンク」で運ぶ
- **`tools/` は開発用**。`index.html` はこれに一切依存しない（1ファイル完結の前提は崩さない）。
  GitHub Pages にも配信されるが、公開リポジトリなので問題ない
- 利用者は iPhone Safari（ホーム画面のアイコン）と PC Chrome の両方で開く。**狭い画面での折り返しを必ず確認する**
- 画面の文言はすべて日本語

## いまの設計（2026-09-21 / v.150）

- **たんぱく質目標＝体重 × 基本係数（初期値1.65 g/kg）**。運動量による加算はしない
  （運動量はエネルギーの必要量と④のバッジにのみ反映）。初期値は体重72.5kg → 目標119.6g。
  **減量期は 1.8〜2.4 g/kg**（除脂肪量基準なら2.2〜2.5）。①の目安表は 1.2／1.4／1.5／1.65／1.8／2.0。
  この型で届くのは**2.3あたりまで（供給の上限は163.4g）**。2.4 では10.6g足りない。
  **止めているのは食塩・飽和ではなく粉の杯数上限（6杯）と候補の上限**（2.4でも食塩7.3g・飽和14.1g）。
  SAVASのときは2.0が限界だったので、Bproで上がった（v.141で実測）
- **週の合格線は固定gではなく「目標の99%」**（`WEEK_P_RATIO`）。
  固定の120gにしていたところ、体重を72.5kgに下げたら目標119.6gを合格線が追い越し、
  「目標を満たしても不合格」になった（v.128で修正）。**体重に連動する値と固定値を混ぜない**
- **プロテインの製品は `PROTEIN` ブロック1つにまとまっている**（v.135）。
  **製品を変えるときはここだけ直す。**杯あたりの値（`SPOON_G`・`SAT_PER_SPOON` ほか）も、
  `PER_SERVE`・`MIN_PER_SERVE`・`WATER_ML`・`MAX_PER_SERVE`・`MIN_SPOONS_TRAINED` も
  すべてここから派生させてある。`tools/crosscheck.js` もこのブロックを読む。
  画面に出る商品名は静的HTMLでは `<span class="pname">`、JS側は `${PROTEIN.name}`
- **いまの製品は Bpro（公表値）**：1回分30g＝すりきり2杯＝たんぱく質20.79g・113.79kcal・
  脂質1.11g・炭水化物5.70g（食物繊維1.08g）・食塩0.64g、水240ml。1杯＝10.395g。
  **水の量は計算に使わないので、杯数ごとの表示には出さない**（v.143）。
  1回分＝2杯に対する量なので、3杯や1杯の行に240mlと添えると数字が合わなかった。
  ①に商品の表示として1回だけ載せ、あとは「水で溶かす」とだけ書く。
  **飽和脂肪酸は非公表なので脂質×60%（`satRatio`）で推定**し、④に推定と明記している。
  **粉代は 3,383円/kg**（`PROTEIN.pricePerKg`・1kg入りの購入価格）＝1杯 約51円。
  粉代は `FOODS` の食費には入れず、③の下のまとめに別立てで出す（`WEEK_COST_AIM` 1,000円は
  食事だけの金額に対する目安）。運動を入力した週は粉代込みで1日平均 約1,282円
- **1回の上限は3杯（31.2g・0.43g/kg）**（`PROTEIN.maxPerServe`）。2杯（20.8g）では
  1食の目安0.4g/kg にもロイシン閾値2.5gにも届かない。吸収の限界ではない。
  **上限は「たんぱく質のg」で決めて杯数に直す**（杯の大きさは製品で変わる）。
  **1日2回まで＝6杯＝62.4g**。初期値は「杯数を指定＝空欄」。
  2食の日の6杯固定も「1回3杯 × その日に許した回数」を超えない。
  なお Bpro では1杯が大きいので、2食の固定（不足62.4g以上）は型のなかでは発動しない
- **粉の杯数にも飽和脂肪酸15gの線を効かせる**（`capSavasBySat`・v.134）。
  候補と油にはガードがあるのに粉だけ無かった。**削るのは2回目から、運動直後の回は上限を保つ**。
  `MIN_SPOONS_TRAINED`（2杯）より下へは削らず、手動で杯数を指定した日は適用しない。
  **Bproでは1杯0.333gと小さいため型のなかでは発動しない**が、製品次第で再び効くので残す
- **杯数は1回目から先に満たす**（`dealSpoons`）。4杯なら3＋1。後ろの回には `MIN_PER_SERVE` を残す。
  回り持ちで配ると4杯が2＋2になり、運動直後の回が上限に届かなかった。
  `MIN_PER_SERVE` は `MIN_SERVE_G`（9.75g）から計算する。杯数で固定すると、
  1杯の大きい製品で後ろの回に取り置きしすぎて1回目が上限に届かなくなる（v.135で踏んだ）
- **1週間の型の骨格は「サバ缶＋ご飯＋プロテイン」**。外食は木曜だけ
  （朝：吉野家 ハムエッグ納豆定食／夕：なか卯 親子丼）。残り6日は
  すき家 まぜのっけ朝食＋自宅のサバ缶。**牛丼・牛めし・豚丼は型から外し**、
  `FOODS` には残して自由選択メニューにしている
- **自動計算は「全栄養素を同時に見る」探索**（`autoQuantities` → `balanceQuantities`・v.144）。
  以前の「栄養素ごとに担当を決めて順番に埋める」やり方は、**担当をまたぐ手が原理的に
  見つからない**。油は7さじが上限なので脂質だけでは20%に届かず、④の脂質はほぼ毎日
  「少ない」だった。総当たりで調べると、同じ品目の範囲で7項目すべてを帯に入れる
  組み合わせが実在した（筋トレのみの日で10,080通り中28通り）
- **動かす品目は10種**：ご飯0〜6杯／店の無料ご飯0〜1／オリーブオイル0〜7さじ／
  ミックスサラダ0〜3皿／ヨーグルト0〜4／納豆0〜3／ツナ缶0〜2／サラダチキン0〜2／
  卵0〜2（メニュー分を引いた残り）／バナナ0〜3本（運動した日は1本以上）／
  アーモンド0〜10粒（v.149。1粒＝1.0g。刻みが細かいのでエネルギーと脂質の微調整に効く）
- **アーモンドの飽和脂肪酸と食物繊維は成分表からの推定値**（v.149）。利用者の提示値に
  無かったが、**0のまま入れると脂質54.1g/100gの品目が飽和15gの制約をすり抜ける**。
  鉄・ビタミンEはこのツールに項目が無いので追っていない
- **点数の順番は 食塩・飽和の線（`BAD_BASE`）＞ たんぱく質の合格線（`PLOW_W`）＞
  ほかの帯（`BAND_W`）＞ エネルギーの近さ（`ENERGY_W`）＞ 食塩・飽和の余裕（`SLACK_W`）
  ＞ 食費（`COST_W`）**。この順番は利用者が決めたもの（余裕優先）。
  たんぱく質を帯と同列にすると「エネルギーを帯に入れるためにPを0.7g落として週を✕にする」
  手が選ばれた。エネルギーは帯の中でも近いほうがよい（`ACTIVITY` の増量余剰を守るため）
- **探索は山登り法**（決まった6点＋散らした `CLIMB_SEEDS` 点、1品目±1 → 動かなければ
  2品目同時）。1回5〜20ms、①の設定を変えたときの再計算全体で約500ms。
  **選べない点には一律 Infinity を返さない** — 平らになって初期点から1歩も動けなくなる
  （v.144で踏んだ）。外れ具合で差を付ける
- **初期点を削りすぎない**（v.146）。速度のために3にしたところ、歩10km＋筋トレの日で
  **脂質が1.3g足りない浅い谷**に落ちた（点数84、最良は19）。
  **減らすときは必ず歩0〜15kmと休養日を掃いて確かめること**
- **初期点を増やすより「揺さぶって登り直す」ほうが効く**（`CLIMB_KICKS`・v.149）。
  品目が10種になって初期点を28に増やしたら①の再計算が1.1秒になった。
  最良点を2〜3品目ずつ動かして登り直す形に替えて、同じ質で約0.8秒に戻した
- **たんぱく質の合格線を割ったときだけ探索を重くする**（v.149）。`pShort` が立ったら
  初期点6倍・揺さぶり3倍でやり直す。ここを割ると週の表が✕になるので、そこだけ時間をかける
- **合格線は「割ったかどうか」で段差を付ける**（`PLOW_FIX`・v.149）。
  重みだけだと、木曜の休養日で「118.3g・エネルギー+318kcal」が
  「118.4g・+386kcal」に**点数で勝ってしまった**（0.1g足りないと週の表は✕になる）
- **帯に入らない日は「探索の失敗」か「型の限界」かを必ず切り分ける。**
  ランダム初期点400個で同じ点にしか行かなければ限界（歩15km以上がこれ。
  必要量3,300kcal超は献立が供給しきれない）
- **`autoQuantities` は条件を鍵にして結果を使い回す**（`balCache`）。
  週の表は7日×前提7通りで1回の描画に十数回呼ばれるが、週の表は `WEEKLY_PLAN` から
  作るので③の数量では変わらない。これが無いと③を打つたびに十数回探索が走る
- **卵は自動計算の対象**（v.144で復帰）。たんぱく質6gと脂質5.2gを同時に足せる数少ない
  品目で、脂質が20%に届かない日に効く。1日2個まで・メニューの卵も数える
- **③の各品目に「今日は使う／使わない」のチェックを置く**（`foodOff`・v.150）。
  外した品目は `readState` で数量0にし、`autoRanges` で上限も下限も0にして探索から外す。
  ④の追加候補・早見表にも出さない。**固定メニューも外せる**（外すとその分を候補が埋め直す）。
  **その日限り**（翌日と「食事をリセット」で全部オン。当日の設定リンクでは運ぶ）。
  **「1週間の型の栄養」は見ない**（`autoQuantities(st, keep, true)`）— あちらは今日の在庫とは
  無関係の基準なので、外した品目があっても型どおりの数字を出す。`balCache` の鍵にも入れる。
  **店のメニューはチェックを品目名の左**に置く（店名は見出しで、そこに付けると店ごと外して見える）
- **「納豆（たれなし）」は自動計算の候補にしない**（v.145）。食塩0gなので候補にすると
  必ずこちらが選ばれるが、**たれをかけて食べるのが基本**という利用者の判断。
  食塩の残り枠が無い日に手で切り替えるための逃げ道として置いてある。
  食塩0gは成分表の「糸引き納豆」の値。ほかの値は「納豆」と同じまま（たれのぶんを含む）
- **65歳になったら `MEAL_P_OPT` を 0.50〜0.55 に上げる**（同化抵抗性。高齢者は1回40g程度まで
  増やすほうが有利）。**基本係数1.65は据え置きで可**（高齢者向けの1.2〜1.6を既に上回る）。
  ①の年齢欄は基礎代謝にしか使っていないので、**自動では入らない**（README「65歳になったら見直すこと」）
- **1食40gの上限は無い**。目安は**体重×0.40 g**（`MEAL_P_OPT`。72.5kgで29g）、
  **体重×0.30〜0.60 g** なら問題なし、**体重×0.60 g 超**で「分散できるなら」と添えるだけ。
  自動分割はしない。**評価帯は体重連動**（v.133。固定gにすると `WEEK_P_MIN` と同じ取り残しが起きる）
- **自動計算の線は品目で分ける。** たんぱく質の候補とオリーブオイルは
  **食塩7.5g・飽和脂肪酸15g**（＝毎日クリアしたい線）を越えない（9.0g・17gは上限であって目標ではない）。
  **ご飯だけは15gでは止めず、上限の17g（`SAT_LIMIT`）で止める**
  （1杯0.1gと小さく、エネルギーの唯一の調整手段。0.1gのために257kcalを捨てない）。
  **どの品目でも上限17gは絶対に越えない。** 足りない分は「残り○g」と表示する
- **必要エネルギーは推定値**。`BMR × 1.63 ＋ 入力した運動の正味消費`（METs−1ベース。
  筋トレは0.032/総負荷量kg、ウォーキング0.5、サイクリング0.28）。
  **筋トレの係数は総負荷量あたりなので、種目の重量やセット数を変えると消費も比例して動く。**
  初期値は v.141 で 総負荷5,658.75kg（181kcal）→ **6,682.5kg（214kcal）** に増えた
  （サイドレイズ・リアレイズ 5.75→8.5kg、チェストプレス 3→6セット。8種目で合計27セット）。
  README の実測表はすべてこの前提で測り直してある
  これは**このツール独自の推定モデル**であって「医学的に正しい消費エネルギーの求め方」ではない。
  **`ACTIVITY` 1.63 は「活動レベル1.50（レベルI＝座位中心）＋ ゆっくり増量の余剰0.13」**を
  1つにまとめた値（v.131）。基礎代謝1,564kcalに対して +203kcal／日 ＝ 週+0.20kg 相当。
  維持に戻すなら1.50、減量（週−0.3kg）なら約1.30。**内訳をコメントに必ず残すこと**。
  活動レベルは集団の平均で、個人の日常活動量を表すものではないと明記する。
  実測とのずれは①の**「必要エネルギーの補正」1欄**（`kcalAdj`・初期値0・±600）で手当てする。
  ご飯の自動計算にも効く。**欄の下に増減ボタン（−100/−50/＋50/＋100/0）を置く** —
  iOS Safari は type=number にスピナーを出さず、テンキーにマイナスが無いため、
  ボタンが無いと iPhone から負の補正を入れられない（v.127）。
  「体重の推移で補正」（入力欄6つ）は v.124 で削除し、
  v.125 で補正値だけを入れる1欄に置き換えた。ずれの計算（体重1kg ≒ 7,200kcal）は利用者が手元で行う
- **基本係数（1.65 g/kg）と `ACTIVITY`（1.63）は別物**。前者はたんぱく質、後者はエネルギー。
  ラベルは「基本係数（たんぱく質 g/kg・運動量にかかわらず同じ）」
  （「運動なしの日」という旧・加算方式の名残は v.125 で解消）
- **食事の時刻の初期値は朝食7:00のみ。昼食・夕食は空欄**（v.128）。
  空欄は基本の間隔で並ぶので、既定では昼食10:00・夕食14:00になる（2食の型は夕食が朝食の8時間後）
- **粉の杯数の指定は初期値が空欄（自動）**（v.128）
- **画面の文言で「運動直後の1回だけ」と言い切らない**（v.142で修正）。
  「プロテインを1日2回まで摂る」は初期値オンなので、①の `liquidNote` が
  すぐ下の `twoNote` と食い違っていた。回数に触れる文は必ず `st.twoDoses` を見る
- **運動の「時刻」は入力しない**（日に何回か運動するため）。②の見出しも「今日の運動・食事の時刻」。
  ⑤の起点は**その日の1食目の時刻**
  （既定は朝食07:00）。粉の1回目の行には時刻を出さず「運動のあとの◯◯（1回目）」とだけ出す。
  2回目は夕食の3時間後なので時刻が出る
- **運動は曜日で固定しない。その日の実績を②に手で入れる。**「8種目×3セット」を毎日の標準にしない。
  消費エネルギーに入るのはチェックの入っている種目だけ。
  **初期値は全種目チェックなし**（v.128）。そのため何も入力していない状態は休養日扱いで
  **粉が0杯**になり、食費が1日約220円上がる（1,079円 → 1,303円）。これは仕様どおり。
  **「運動なし」と「未入力」を区別する**（`exDate`。有酸素の距離か筋トレの欄を触るか、
  **「計算する」を押す**と入力済みになる。自動再計算では確定しない）。
  **ボタン類は置かない** —「今日はこの内容で確定」「今日は運動していない」、有酸素と筋トレの
  プリセットは v.122 で削除した。毎日手入力するので不要、というのが利用者の判断
- **合否は「週平均」ではなく「各日」で見る**。③の下の表は2つに分ける。
  「1週間の型の栄養（食事だけの値）」＝Pその日の目標の99%以上・食塩7.5g未満・飽和15g以下、
  「運動の実績と、必要エネルギー」＝運動の前提（`WEEK_EX`）ごとに必要量との差を出す
- **画面の並びは ① 基本設定 → ② 今日の運動・食事の時刻 → ③ 今日の食事 → ④⑤⑥**（v.147）。
  運動を入れると③の数量が自動で決まるので、**上から順に入力すると手戻りが無い**という利用者の判断。
  丸数字は位置に合わせて振り直してある（運動が②・食事が③）。README と画面の文言も同時に入れ替えること。
  **一括置換すると「列挙の丸数字」まで入れ替わる**（⑥のポイントの番号、ACTIVITYの内訳①②、
  実測値の①②、①総量＞②1回あたり＞③タイミング）。v.148で6か所直した。次に並びを変えるときは、
  丸数字がセクション参照か列挙かを1つずつ見ること
- 食塩・飽和脂肪酸・エネルギーは**段階表示**（詳細は README の「判定の段階」）
- **④の栄養成分の各行には「多い順に3品目」を添える**（`topN`）。
  脂質とエネルギーだけ抜けていたので v.138 で揃えた。**エネルギーは単位が kcal**なので
  `topN(kcal, 'kcal')` と単位を渡す（g のつもりで出すと「ご飯 1,285g」になる）。
  エネルギーの行は「摂った側の内訳」と「必要量の内訳（基礎代謝×`ACTIVITY`＋運動）」を行で分ける
- **④の各行は、基準内でも線までの距離を必ず数値で書く**（v.139）。以前は基準内のときだけ
  書式がばらついていた（脂質・炭水化物は無言、飽和は文だけ、食塩とエネルギーは数値あり）。
  **越えない線（飽和・食塩）は「あと○g」ではなく「余裕○g」**と書く — 「あと」は
  埋めてよい枠に読めるため。飽和の「埋めにいく必要はありません」はそのまま残す。
  **範囲の行（脂質・炭水化物）は近いほうの線までを出す**（`inRange`）。
  遠いほうを出すと余裕があるように見える。上限ぴったりの日に「余裕 0g」と出るのが狙い
- 1食の目安量は割合ではなく**gで等分**

## 作業の進め方

1. **まず `README.md` と `index.html` を読む。** 会話の記憶ではなくファイルが正
2. 編集は Node のスクリプト（`fs.readFileSync` → 文字列置換 → `writeFileSync`）で行う。
   scratchpad に置く。bash のヒアドキュメントはバッククォートを壊すので `Write` ツールで作る。
   **読み込んだら必ず `s.replace(/\r\n/g, "\n")` で改行を正規化する。**
   `git checkout` で戻したファイルは CRLF で復元されるため、LFで書いたパッチが
   1件も一致しなくなる（複数行の置換が黙って空振りする）
3. `index.html` を変えたら **`APP_VERSION` を上げる**（`2026-09-01.NNN` 形式）
4. 構文チェック：`node -e "const s=require('fs').readFileSync('index.html','utf8');new Function(s.match(/<script>([\s\S]*)<\/script>/)[1])"`
5. **ブラウザで実際に動かして確認する**（`file:///D:/Project/repos/protein-sync/index.html`）。
   プレビューは `data:` URL のため `localStorage` が例外になる。必要なら差し替えてテストする
6. **ランダム検証を回す**：`tools/browser-check.js` をコンソールに貼って `PSCheck.run(1000)`。
   曜日・体重・基本係数・食事回数・有酸素・粉の設定・食事の数量を振り、例外・`NaN`・
   表示の矛盾・上限超過が無いことを確認する。終わったら `PSCheck.restore()`
7. `README.md` を同じ内容に直す（数値も実測値に置き換える）
8. **`README.md` か `index.html` を直したら `node tools/crosscheck.js` を通す**（0件になるまで）
9. コミットしてプッシュ。GitHub Pages の反映は `gh api repos/achaboo/protein-sync/pages --jq .status`

## 検証でよく使う書き方

```js
// 曜日を固定する
const RealDate = Date, base = new RealDate(2026, 8, 14);   // 月曜
window.Date = class extends RealDate {
  constructor(...a){ a.length === 0 ? super(base.getTime()) : super(...a); }
  static now(){ return base.getTime(); }
};
applyDailyDefaults();
[...document.querySelectorAll('button')].find(b => /計算する/.test(b.textContent)).click();
```

数量を変えるときは `el.value = v` だけでなく `el.dispatchEvent(new Event('input', {bubbles:true}))`
まで行う（上限の切り詰めや自動計算がこのイベントで動くため）。

## 注意

- 栄養の数値は健康に直結する。**公表値を確認してから入れる**。推定値は推定と明記する
- 週の結果の数値を README に書くときは、**実際にツールを動かして測った値**を使う
- 利用者は健診でLDLの上昇・動脈硬化指数3.3を指摘されている。
  たんぱく質を満たすために食塩や飽和脂肪酸を増やす解決はしない
